version: '3'

services:
  app:
    image: my-nodeapp:1.0
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/urlshortener

  mongo:
    image: mongo
    ports:
      - "27017:27017"